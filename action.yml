name: kubectl-eks-aws-iam-authenticator
description: "This action provides kubectl for Github Actions. This action includes aws-iam-authenticator for accessing Amazon EKS."
author: 'Lucien Martijn'
inputs:
  namespace:
    description: 'The name of a namespace of the physical cluster'
      required: true
runs:
  using: 'docker'
  image: 'Dockerfile'
